syntax = "proto3";

package smtp;
option go_package = ".;smtp";

message Message {
  string htmlBody = 1;
  repeated string emailTo = 2;
}

message Nothing {
  bool dummy = 1;
}

service SmtpService {
  rpc Send(Message) returns (Nothing);
  rpc Stop(Nothing) returns (Nothing);
}
